# Directive Registry

이 저장소 자신의 결정·지시를 누적 색인하는 문서 — 형식과 원칙은
[`ko/02-directive-registry.md`](../ko/02-directive-registry.md) 크리스탈을
따른다. 아직 이 저장소를 관리하며 내린 결정이 없어 표가 비어 있다 —
새 결정이 생길 때마다 아래 표에 한 행씩 추가한다(번호는 영구 ID, 낮은
번호가 항상 우선한다는 뜻은 아니다 — 자세한 충돌 처리 규칙은 크리스탈
02번 참고).

`scripts/agent-ops-framework-reflection-check.py`가 이 표를 스캔해
`ko/REFLECTION-CANDIDATES.md`에 편입 후보를 자동으로 쌓는다.

| # | 지시/원칙 내용(굵게) + 실제로 뭘 했는지 | 재적용 트리거 | 사용자 지시 원문 |
|---|---|---|---|
| 1 | **`translated-from` 스탬프를 git 커밋 해시가 아니라 콘텐츠 해시(SSOT 본문 sha256 + 번역본 자기 본문 sha256)로 설계한다 — history-rewriting에 구조적으로 영향을 안 받게** — 경위: 이번 세션 중 두 차례 squash 뒤 en/ 44개 중 37개의 스탬프가 도달 불가능한(orphan) 커밋을 가리키고 있었음을 발견. 1차 대응(`git merge-base --is-ancestor` 도달가능성 검사 + `--repair` 옵션)은 감지·복구만 자동화했을 뿐 "재작성마다 사람이 기억해서 실행"해야 하는 근본 취약점을 남겼다 — 사용자가 "구조가 강건하지 않다"고 재지적해 `scripts/agent-ops-framework-translation-sync-check.py`를 콘텐츠 해시 기반으로 통째로 재설계함(git 객체를 아예 참조 안 함). 같은 squash를 스크래치 클론에서 재현해 재설계 전 37/44 깨짐 → 재설계 후 0/44를 라이브로 확인. | 새 번역 파일을 만들거나 기존 크리스탈의 SSOT/번역 스탬프를 다룰 때마다(이제 history-rewriting은 재적용 트리거가 아니다 — 애초에 안 깨지므로) | "최근에 커밋 병합 작업들을 하다 보니 파일에 명시된 커밋 해시가 깨지는 이슈가 있을 것 같다. 그러한가? 해결하고, 이를 어떻게 방지할지 고도화 필요하다" / "git commit 병합하면 바로 깨지게 되니까 지금 구조가 강건하지가 않다." / "더 강건한 방법을 고안해서 적용해야 한다." |

# Heuristics archive — research-digest-agent

> Rules moved out of `heuristics.md` once the active-rule cap (7, set low for this 10-week demo — crystal 06's real guidance is 10-12) was exceeded. Kept here, not deleted, so a later recurrence can be restored instead of re-added as a near-duplicate.

## Restored (no longer archived)

- **Do not let an alarming/urgent-sounding title override the interest-keyword filter.** — restored: a near-duplicate lesson was proposed again, matched against this archived entry instead of being added as new (crystal 06's backtrack condition).

